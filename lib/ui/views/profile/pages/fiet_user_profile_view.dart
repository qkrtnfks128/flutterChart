import 'package:flutter/material.dart';

class FietUserProfileView extends StatelessWidget {
  const FietUserProfileView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text(""),
            Text(""),
            Text(""),
            Text(""),
            Text(""),
          ],
        ),
      ),
    );
  }
}
